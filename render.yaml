services:
  - type: web
    name: your-bot-name
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py  # ✅ Changed from gunicorn to python
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: ADMIN_CHAT_ID
        sync: false
      - key: TARGET_CHAT_ID
        sync: false
      - key: WEBHOOK_URL
        sync: false
      - key: SOURCE_CHAT_ID
        sync: false
